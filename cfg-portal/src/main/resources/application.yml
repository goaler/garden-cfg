server.port: 5019

# 数据库配置
spring.datasource:
    url: jdbc:mysql://localhost:3306/garden_cfg?serverTimezone=UTC&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis.config= classpath:mybatis-config.xml
mybatis.mapperLocations: classpath:org/garden/cfg/core/repository/mapper/*.xml
logging.level.org.garden.cfg.core.repository: debug

spring:
  mvc:
    view:
      suffix: .html
      prefix: /page/